const should = require('should');
const sinon = require('sinon');
const fusionsController = require('../controllers/fusionsController');

describe('Fusion Controller Test:', () => {
	describe('Get', () => {
		it('should not allow empty term and location', () => {

		});
	});
});
